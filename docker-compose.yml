services:
  api:
    build: .
    env_file: .env
    ports:
      - "8140:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      FRAUDGRAPH_API_URL: http://api:8000
    ports:
      - "8641:8501"
    depends_on:
      - api
